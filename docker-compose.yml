services:
  time-service:
    build: ./services/time-service
    container_name: time-service
    restart: unless-stopped

  weather-service:
    build: ./services/weather-service
    container_name: weather-service
    restart: unless-stopped

  chatgpt-quote-service:
    image: alpine
    command: sh -c "while true; do echo 'Quote placeholder'; sleep 60; done"
